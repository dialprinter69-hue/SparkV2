<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Dark variant: dark base + near-black window background, no white flash. -->
    <style name="Theme.SparkV2" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/spark_window_bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
